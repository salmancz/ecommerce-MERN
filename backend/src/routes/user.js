const express = require("express");
const router = express.Router();

router.post("/signup", (req, res) => {}); // end of router.post

router.post("/signin", (req, res) => {}); // end of router.post

module.exports = router;
